import { TPrepareCtx, TPrepare, TVmap } from './interface.js';
export declare const prepare: TPrepare;
export declare const vmap: TVmap;
export declare const _clone: <T = any>({ value, id, cwd, root, resource, memo, seed, vmap, prefix }: TPrepareCtx<T>) => T;
